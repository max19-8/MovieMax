package com.example.moviemax.view.adapter.adapterdata

import com.example.moviemax.databinding.ShimmerPlaceholderLayoutBinding
import com.example.moviemax.view.adapter.base.BaseDataHolder

class ShimmerViewHolder(binding: ShimmerPlaceholderLayoutBinding): BaseDataHolder<String>(binding)  {
    override fun bind(items: String) {
    }
}